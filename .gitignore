.gradle/
.idea/
.kotlin/
build/
app/build/
local.properties
*.iml

# Claude Code local runtime artifacts (locks, machine-local state)
.claude/scheduled_tasks.lock

# Python bytecode (scripts/scoring + exploraciones) — regenerable, no se versiona
__pycache__/
*.pyc

